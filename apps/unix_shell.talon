tag: terminal
-
# Custom commands

grep: "grep "
make dir: "mkdir "
manage <user.text>:
    "./manage.py "
    user.insert_formatted(text, "SNAKE_CASE")
ssh: "ssh "
ssh add: "ssh-add "
tail: "tail "
t dev: "tdev "
# extra space so vim doesn't think this is a modeline:
vim : "vim "
